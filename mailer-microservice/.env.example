PORT=
SECRET=

# SMTP transport — configure for your provider (Gmail/SES/SendGrid/Mailgun/…).
# Defaults: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_SECURE=false (STARTTLS).
# Use SMTP_PORT=465 + SMTP_SECURE=true for implicit TLS.
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
EMAIL_USER=
EMAIL_PASS=

REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
